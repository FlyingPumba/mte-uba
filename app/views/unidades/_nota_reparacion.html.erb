<% if @nota_reparaciones.any? %>
<div class="panel panel-default">
    <div style="min-height: 50px;" class="panel-heading">Notas de Reparación
    </div>
    <div class="panel-content">
    <center>
        </br>
        <table class="table table-hover table-striped">
          <thead>
            <tr>
              <th><%= model_class.human_attribute_name(:fecha) %></th>
              <th><%= model_class.human_attribute_name(:numeroserie) %></th>
            </tr>
          </thead>
          <tbody>
            <% @nota_reparaciones.each do |nota_reparacion| %>
              <tr>
                <td><%= nota_reparacion.fecha %></td>
                <td><%=link_to (nota_reparacion) do %> <% nota_reparacion.numeroserie %> <% end %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <%# will_paginate(@nota_pedidos) %>
        </center>
    </div>
</div>
<% end %>
