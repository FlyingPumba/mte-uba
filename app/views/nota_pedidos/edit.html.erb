<% content_for(:breadcrumbs) do %>
    <%= breadcrumbs do |b|
      b.item current_taller.nombre.titlecase, root_path
      b.item @comprobantable.class.model_name.human, @comprobantable.class
      b.item @comprobantable.full_name, @comprobantable
      b.item "Editando Nota Pedido", @comprobantable, :active => true
    end %>
<%end%>

<% content_for(:sidebar) do %>
  <div class="block">
      <div class="navigation">
        <ul>
          <li class="fa fa-eye fa-lg"><%= link_to( "Mostrar", [@comprobantable, @nota_pedido] ) %></li>
        </ul>
      </div>
  </div>
<% end %>

<%= render 'form' %>
